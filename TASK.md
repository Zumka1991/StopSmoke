# 📝 Заметки разработчика: StopSmoke

Этот файл содержит информацию о проекте, инструкции по запуску и план развития.

## 🛠 Технологический стек

### Backend
- **Framework**: .NET 10 (Preview) / ASP.NET Core
- **Database**: PostgreSQL
- **ORM**: Entity Framework Core
- **Auth**: JWT Bearer + ASP.NET Core Identity
- **API**: REST Controllers + OpenAPI (Swagger)

### Frontend
- **Framework**: React 19
- **Build Tool**: Vite
- **Language**: TypeScript
- **Styling**: Vanilla CSS (с переменными в `index.css`)
- **Icons**: Lucide React
- **I18n**: i18next (поддержка мультиязычности)
- **HTTP Client**: Axios

### Infrastructure
- **Docker**: Docker Compose для запуска всех сервисов (Backend, Frontend, Postgres, Certbot)

---

## 🚀 Запуск проекта

### Вариант 1: Docker Compose (Рекомендуемый)
Запуск всего окружения одной командой:
```bash
docker-compose up --build
```
- Frontend: http://localhost:80
- Backend: http://localhost:5216
- Swagger: http://localhost:5216/swagger

### Вариант 2: Локальная разработка

#### Backend
1. Перейдите в папку `backend`:
   ```bash
   cd backend
   ```
2. Настройте строку подключения к БД в `appsettings.Development.json` или используйте локальный Postgres.
3. Запустите:
   ```bash
   dotnet watch run
   ```

#### Frontend
1. Перейдите в папку `frontend`:
   ```bash
   cd frontend
   ```
2. Установите зависимости (если еще нет):
   ```bash
   npm install
   ```
3. Запустите dev-сервер:
   ```bash
   npm run dev
   ```

---

## 📂 Структура проекта

### Backend (`/backend`)
- **Controllers/**: API эндпоинты (Auth, Profile, Marathon, Relapse, etc.)
- **Models/**: Entity модели (User, Marathon, Relapse)
- **Data/**: DbContext и конфигурация EF
- **Services/**: Бизнес-логика (например, `MarathonCompletionService`)
- **Migrations/**: Миграции базы данных

### Frontend (`/frontend/src`)
- **pages/**: Основные страницы приложения
  - `DashboardPage`: Главная панель
  - `MarathonPage`: Управление марафонами
  - `LeaderboardPage`: Таблица лидеров
  - `ProfilePage`: Профиль пользователя
  - `SecretAdminPage`: Админка
- **components/**: Переиспользуемые UI компоненты
- **api/**: Настройка Axios и запросы к API
- **locales/**: Файлы переводов (i18n)

---

## ✅ Текущий статус и задачи (Roadmap)

### Реализовано
- [x] Аутентификация (Login/Register)
- [x] Профиль пользователя
- [x] Отслеживание срывов (Relapse)
- [x] Марафоны (участие, прогресс)
- [x] Таблица лидеров
- [x] Админ-панель (базовая)
- [x] Docker контейнеризация

### Планируется / Идеи для улучшения
- [ ] **Тестирование**: Добавить Unit и Integration тесты для Backend и Frontend.
- [ ] **UI/UX**: Улучшить дизайн, добавить анимации (framer-motion?).
- [ ] **Notifications**: Добавить уведомления (email или push) о достижениях.
- [ ] **Gamification**: Расширить систему ачивок и наград.
- [ ] **Mobile**: Адаптация под мобильные устройства (PWA).
- [ ] **CI/CD**: Настроить GitHub Actions для авто-деплоя.

---

## 💡 Полезные команды

**Миграции (Backend):**
```bash
dotnet ef migrations add <MigrationName>
dotnet ef database update
```

**Линтинг (Frontend):**
```bash
npm run lint
```
